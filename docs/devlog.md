# 開発記録

調査した内容・悩み疑問・学習した内容・設計や仕様の決定事項を、区切りごとに記録していく。
新しいエントリはこのファイルの一番上(このテンプレートのすぐ下)に追記していく。

---

## 次にやること(TODO)
### 口頭ディフェンス後の指摘・要望
- リアクションのトグル化(一回だけ押せるようにする)【完了】
- コメントも検索対象にする【完了】
- 管理者ログインの認証のセキュリティを完璧(強固)にする【完了】
  - 方針: DBのセッションテーブル方式(`AdminSession`・`LoginAttempt`を追加)。署名付きCookieはDBなしで済むが回数制限・個別失効ができないため採用しなかった。パスワード自体は変更しない
  - 破られうるパターンと対応(危険度順)
    1. 【高】環境変数`ADMIN_PASSWORD`が未設定だと、空文字のハッシュ値(固定値)をCookieに入れるだけで管理者になれる(`?? ""`でfail-open) → 解消。パスワード由来のCookie値を廃止し、未設定・空なら必ず拒否する
    2. 【高】ログイン失敗に回数制限・ロックが無く、総当たりし放題(コードレビュー指摘#4) → 解消。同じIPから15分間に5回を超えたらロック。IPを変える攻撃には効きにくい
    3. 【高】認証Cookieが「固定のハッシュ値」で、漏れたら期限無視・永久に再利用できる(コードレビュー指摘#5) → 解消。毎回変わるランダムなトークンにし、期限はサーバー側で確認、ログアウトでDBの行を削除して即無効にする
    4. 【中】ソルト無しSHA-256のハッシュが漏れると、オフラインでパスワードを逆算される → 解消(パスワード由来の値をCookieに入れない)
    5. 【低】`!==`/`===`による通常の文字列比較はタイミング攻撃の理論上の余地がある → 解消。`crypto.timingSafeEqual`で比較
    6. 【中】失敗したログインの記録・通知が無く、攻撃に気づけない → 一部解消。失敗は`LoginAttempt`に記録される(古い記録は自動で削除)が、通知はしない
  - 残っている限界: パスワードの強度は変更していない/IPを変える攻撃・IPv6のアドレスを変える攻撃には効きにくい/同じ公開IPを共有するネットワークでは回数制限が共有される/盗まれたトークンはログアウトか期限まで有効/同時送信で通せる件数を抑える点は、実際の同時送信では未テスト
  - 本番反映時の注意: pushすると、Vercelのビルド(`prisma migrate deploy`)で本番のNeonに新しい2テーブルが作られる。既存のログインは無効になり、再ログインが必要


---

## 2026-09-08

### 調査した内容
- Next.jsの`cookies()`の仕様をドキュメントで確認(非同期API、`.set()`はServer Action/Route Handler内でしか使えない)
- `useActionState`の型エラーの原因を調査した(login関数の戻り値を型推論でしていた)
- 多層防御の仕方(errorとして返すか、redirectさせるか)

### 悩み・疑問
- Server Actionは管理画面のページを経由せず、直接POSTを送れば(curlなど)実行できてしまう。ページ側の認証チェックだけで十分か悩んだ
- パスワードのハッシュ化になぜSHA-256を選んだのか(RSAとの違い)を整理する必要があった
- ソフトデリートにしているので、削除してからまた公開させるように設計するか悩み中
- 並び替えをリアルタイム(onChange)でやったが意味あったのか

### 学習した内容
- Server Actionは独立したエンドポイントなので、呼び出し元がどこであれ実行される。ページ側の認証チェックだけでは不十分で、Action自身の中でも認証確認が必要(多層防御)
- `useActionState`は、渡す関数の「引数の型」と「戻り値の型」を一致させる必要があり、ズレるとTypeScriptの型エラーになる
- SHA-256は一方向のハッシュ関数で、RSAのような「元に戻せる暗号化」とは目的が違う。パスワードを復元不可能な形でCookieに持たせたい今回はハッシュが適切
- 多層防御の仕方は正解はないが、理由がある設計方法にする（今回は不正な入力になる可能性のものをerrorとして返している）

### 設計
- 認証用のテーブルは作らずCookie+環境変数の照合だけで済ませる
- Cookieには平文パスワードではなくSHA-256のハッシュ値を保存し、照合もハッシュ同士で比較する
- ログインフォームだけ`useActionState`を使うClient Componentにして、他はServer Component+Server Actionで統一する
- 認証チェックは`admin/page.tsx`(表示時)と各Server Action内(操作実行時)の両方で行う
- 編集画面は動的ルート /admin/posts/[id]/edit として実装し、findUniqueで該当投稿を取得、存在しなければnotFound()で404にする
- 編集画面でコメントの削除を追加した
- 並び替えをリアルタイムで行えるようにした

### 仕様
- 管理者はユーザー名なしの固定パスワードでログインする
- ログイン後は同じブラウザで12時間は再入力不要
- パスワードは環境変数`ADMIN_PASSWORD`で管理する
- パスワードを間違えたら画面遷移せずフォーム内にエラーメッセージを表示する
- 投稿削除はソフトデリート(`deletedAt`を立てるだけで物理削除はしない)
- 管理者は投稿一覧の「編集」リンクから、投稿のニックネーム・本文を編集できる
- 並び替えを選択したときにリアルタイムに反映されるようにしている

## 2026-09-09

### 調査した内容
- vercelについて
- NeonとNeonとSupabaseについて
- vercelを使うにあたってのコードの変更（環境構築）

### 悩み・疑問
- vercelと接続させるdbの選定(NeonとSupabase)
- vercelはdocker環境じゃないため、gitに送らないクライアントとかをどうするか
- PreviewとProductionでDBを共有するか分離するか
- returnの中でreturn(<ul>の中<li>の情報をreturnでまとめて返すみたいな感じ)のコードにするか、見やすさ重視のコードにするか
- 名前を入れなかった人をJSXで名無しと表示しているが検索で名無しを調べられないから許容かどうか

### 学習した内容
- ビルド→nectjsからjsに変換、効率よく読み込めるようになんかする(バンドル)
- デプロイ→ビルドでできた完成品をインターネットのサーバーにおく。ここまでの作業はgithubにpushした時点でvercel側がやっている
- envの情報をgithubは持っていないため、その情報をvercelに渡す作業が必要
- NeonはprosgresSQLのdbサービスで簡易的な操作が可能。gitのような動きができる(コミットやブランチみたいな)
- SupabaseはprosgresSQL専用ではなく総合dbサービス。メリットはユーザー認証の雛形があるや画像や動画を保存できる
- 元々PreviewとProductionでDBを共有するつもりだったが、Neonの機能でPreviewだけ自動的に専用DBブランチを持たせることができたのでそれを採用

### 設計
- 本番用DBはローカル(Docker)とは別に、Neonを用意する
- Vercelのdevelopブランチ→Preview環境、mainブランチ→Production環境、という対応にして今の運用と揃える
- まずはGit連携を試すだけにして、確認後は連携を切る(デプロイ回数を抑えるため)
- ReactionTypeマスタデータはNeonのSQL Editorから手動で投入する運用にした
INSERT INTO "ReactionType" (name, emoji) VALUES
  ('いいね', '👍'),
  ('わかる', '🙌'),
  ('ほっこり', '🥰');

### 仕様
- 個人開発規模なのでVercel Hobby(無料・非商用利用)の範囲で運用する
- 環境変数(DATABASE_URL・ADMIN_PASSWORD)はVercel側にも別途設定する必要がある
- mainブランチ=Production、developブランチ=Preview(専用DBブランチ)という対応

## 2026-09-10

### 調査した内容
- 検索での名無しさん(db上のnull)をどうヒットさせるか
- Tailwind CSSについて
- vercelとローカルの挙動の違い

### 悩み・疑問
- yamlに環境設定をそのまま書いている
- dbのデフォルトでnullを名無しさんすることは可能、しかし編集(updete)でデフォルトが反応しないため実装したいならコード上で行う必要あり
- dbではなくそもそも名無しさんを検索した時にnullをヒットさせる方法もあるが、その場合「名無し」で入力したときなどの部分検索ができない、又は全通りを書く必要がある
- エラー処理がvercelだと思った通りにできてない(エラーを投げるとおそらくnectjsのデフォエラー画面)
- 編集にはそもそも文字制限を設けていなかったため

### 学習した内容
- どちらもデメリットが存在するため、部分検索ができる方(dbで名無しさんを入力)を採用する
- db上でトリガーを使えばできるが、db上でifのような動きは分かりづらくなるため今回は不採用
- nextjsの復習
- Tailwind CSSの細かな設定を色々調べた
- おそらく見えない文字がvercelの入力欄に入っていたからなはず

### 設計
- envファイルにPOSTGRES_PASSWORDなどを追加（セキュリティ）
- env.exampleファイルに雛形を載せている
- 入力時にニックネームをnullにするとdbで「名無しさん」が入るようにする
- 編集に文字制限を設けた(dbと同じ140文字)

### 仕様
- env.exampleファイルはgitに公開しても問題ないローカルの情報と仮のURLとpasswordを入れている
- updateの際にニックネームをnullに変更した場合、コード上で判断しdbに明示的に名無しさんを入れる
- 見えない文字が入らないようバリデーション前にtrim()を入れるようにした

## 2026-09-11

### 調査した内容
- UIチェック
- claude code コマンド調べ
- 要件定義やER図、物理設計の手直し
- promiseとpassive: trueの概念

### 悩み・疑問
- 管理者投稿一覧画面で本文を多く折り返して表示しているから見づらい
- いいねを一回だけにするか(Cookieだから厳密ではない)
- フッターとヘッターを常に表示する形にするか
- promiseとpassive: trueこれもうちょい勉強

### 学習した内容
- 折り返す代わりに...を使う方法(truncate)
- claude codeのコマンドでできることが増える。(画面を作る前にデザインを作る、コードレビューをしてもらう)


### 設計
- 投稿一覧のニックネームが長い場合、truncateで...で省略
- 管理者画面はニックネームと本文をtruncateで...で省略
- vercelで予期しないエラーがきたからエラー画面をあらかじめ作っておく
- childrenでのエラー用のファイル(error.tsx)とchildrenより外のエラー用のファイル(global-error.tsx)を作成
- いいねは回数制限を設けない
- 管理者画面で削除済みのものを復元できるようにした

### 仕様
- 画面に対しニックネームが長い場合、...で省略する
- 管理者画面の省略は編集の時に全文が見えるためこの画面では省略してもいいと判断した
- エラーでnectjsの標準のエラー画面ではないようにファイルを作成した(404は別)
- いいこと日記のため、いいねを多くもらうことに関して悪いことがないでのいいねの回数制限は必要がないと判断した
- 元々ソフトデリート(deleteAtがnullかどうか)だったため、それをnullに変更できる復元ボタンを作成した(管理者画面)

## 2026-09-14

### 調査した内容
- フッターとヘッターをどのように使うか
- 自分の投稿を消せるかどうか
- 環境構築理解(Dockerfile,docker-compose,deccontainer,pakage.json)
- claude codeのコマンドでcode reviewを行った

### 悩み・疑問
- 常に表示させるか、トップページに戻るようにするか
- 自分の投稿を編集や消すことをできるようにするか
- アカウントを作る方法は発表の時間も考えてやりたくない。やれることはクッキーを使う方法か投稿ごとにパスワードを設定してもらう方法(2ch,5chがこの方法らしい)
- code reviewで10件も指摘された。できるところ、諦めるところを考える

### 学習した内容
- 今回のような下まで見て欲しい時は、常に表示させる方法はあんまりかもしれない。
- またmainの日記を見れる範囲もフッターとヘッターによって小さくなるデメリットも考えないといけない
- どちらの方法でもデータベースのカラムを追加が必要。毎回パスワードを考えるのは使用者目線でも面倒だと判断し、クッキーの方がいいが、クッキーも時間で変わったり、
別ブラウザで立ち上げると自分の投稿だと判断できないためどちらも不採用。
- code reviewをかなりの精度でしてくれるからしておこう
- それぞれのコマンドについて理解することができた
- pnpmのインストールについてバージョンを今回固定していなかったので、packageManagerで固定した。(pnpm-lockがあるからほぼほぼリスクないけど一応)

### 設計
- フッターに画面の一番上に移動できるボタンを作成
- 今回は自分の投稿を編集、削除する機能はつけない。
- コメントの削除の取り消しボタンを作成した
- 最大文字数などを各ファイルで設定していたがまとめて一つのファイルに設定した。(lib/constants.ts)

### 仕様
- ページの300Yになったら画面の右下にその画面の一番上に移動するボタンが出るようにした。（押したらゆっくり一番上に移動される）
- 本文の削除取り消しはあったのにコメントの削除取り消しがなかったため追加した。
- 各ファイルで設定していたため衝突する危険があるため、一つのファイルにまとめた。

## 2026-09-15

### 調査した内容
- pakage.json
- tsconfig.json
- next.config.ts
- src/lib/prisma.tsのシングルトンパターン

### 悩み・疑問
- 前回使用していた環境構築のまま特に考えずに進めていたが、意外と使ってない環境設定が多いなと感じた。色々できるようにあらかじめ広めに作ってるって感じだった

### 学習した内容
- Docker関連以外は見てなかったが、コンパイルのエラーを表示させたりしているのがtsconfigなので、ある程度知っている必要があると思った。
- isolatedModulesがあるため、型だけのexportにはexport typeが必要(SWCが1ファイルずつ変換するため)
- globalThisを使ったPrismaClientのシングルトンパターンで、開発中のホットリロードによるDB接続過多を防いでいる

### 設計
- pnpm-workspace.yamlの説明できないignoredBuiltDependenciesを、動作確認した上で削除して整理した

### 仕様
- 特に変更なし


## 2026-09-16

### 調査した内容
- いいね数の数え方(filterでしてたけどいいね数を多くできるように設定してたからよくないと思った)
- コードチェック

### 悩み・疑問
- いいね数をdb上で計算をするようにしたが毎回値が変わるたびにこの計算が起こるため、どうしようか悩み中
正直このくらいのデータ量だと誤差だから考えなくて良さそう
- 

### 学習した内容
- groupByを使って計算することでdb上でいいね数を数えることができる

### 設計
- groupByでいいね数を計算できるようにしている
- Cookieを使う場面がloginしかなくなったためCookieを有効にする範囲を全体からadminに変更

### 仕様
- 見た目は変わってない(Cookieも同じく)


## テンプレート(コピーして使う)

## YYYY-MM-DD

### 調査した内容
-

### 悩み・疑問
-

### 学習した内容
-

### 設計
-

### 仕様
-

---
