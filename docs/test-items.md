# テスト項目書

手動での動作確認用チェックリスト。実装完了後の最終確認として作成。

「実際の結果」「判定」列は、実際にテストを行うタイミングで記入する。

## 1. 投稿機能(タイムライン)

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 1-1 | 通常の投稿ができる | タイムライン画面を表示 | ニックネーム・本文を入力し投稿する | ニックネーム「たぬき」、本文「今日は良い天気だった」 | タイムラインの先頭に投稿が表示される | | |
| 1-2 | ニックネーム未入力でも投稿できる | タイムライン画面を表示 | ニックネームを空欄のまま本文だけ入力し投稿する | 本文のみ「테스트」 | ニックネームが「名無しさん」として表示される | | |
| 1-3 | 本文が空だと投稿できない | タイムライン画面を表示 | 本文を空欄のまま投稿する | 本文なし | エラーになり投稿されない(またはエラー画面が表示される) | | |
| 1-4 | 本文の文字数上限(140文字)ちょうどは投稿できる | タイムライン画面を表示 | 140文字ちょうどの本文で投稿する | 140文字の文字列 | 投稿が成功する | | |
| 1-5 | 本文が文字数上限(140文字)を超えると弾かれる | タイムライン画面を表示 | 141文字以上の本文で無理やり送信する(devtoolsでmaxLength属性を外すなど) | 141文字の文字列 | サーバー側でエラーになり投稿されない | | |
| 1-6 | 投稿の改行がそのまま表示される | タイムライン画面を表示 | 本文に改行を含めて投稿する | 「1行目\n2行目」 | 表示上も改行された状態で見える | | |

## 2. リアクション機能

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 2-1 | リアクションボタンを押すと件数が増える | 投稿が1件以上ある | 任意の投稿の👍ボタンを押す | - | 👍の件数が1増える | | |
| 2-2 | 同じ絵文字に複数回反応できる(上限なし) | 投稿が1件以上ある | 同じ投稿の同じ絵文字ボタンを複数回押す | 3回押す | 押した回数分だけ件数が増える(3増える) | | |
| 2-3 | 絵文字ごとに件数が独立している | 投稿が1件以上ある | 👍と🙌をそれぞれ1回ずつ押す | - | 👍と🙌それぞれの件数が別々に1ずつ増える | | |
| 2-4 | 「多い順」で並び替えると件数順になる | リアクション数が異なる投稿が複数ある | 並び替えで「👍が多い順」を選ぶ | - | 👍の件数が多い投稿から順に並ぶ | | |

## 3. コメント(返信)機能

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 3-1 | 通常のコメントができる | 投稿が1件以上ある | コメント欄に入力して送信する | 「わかります!」 | その投稿の下にコメントが追加表示される | | |
| 3-2 | コメント本文が空だと送信できない | 投稿が1件以上ある | 本文を空欄のまま送信する | 本文なし | 送信できない(`required`属性、またはサーバー側エラー) | | |
| 3-3 | コメントの文字数上限(30文字)を超えると弾かれる | 投稿が1件以上ある | 31文字以上のコメントを無理やり送信する | 31文字の文字列 | サーバー側でエラーになる | | |
| 3-4 | コメントは投稿順(古い順)に並ぶ | 投稿にコメントが複数ある | 複数コメントした投稿を見る | - | 投稿した順番(古い→新しい)に並んでいる | | |

## 4. 検索・並び替え機能

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 4-1 | 本文の部分一致で検索できる | 「コンビニ」を含む投稿がある | 検索欄に「コンビニ」と入力し検索 | 「コンビニ」 | 該当する投稿だけが表示される | | |
| 4-2 | ニックネームの部分一致で検索できる | 「たぬき」という投稿者がいる | 検索欄に「たぬき」と入力し検索 | 「たぬき」 | 該当する投稿だけが表示される | | |
| 4-3 | 該当が無い場合は「見つかりませんでした」と表示される | - | 存在しないキーワードで検索する | 「xxxxzzzz」 | 「見つかりませんでした」という趣旨のメッセージが表示される | | |
| 4-4 | キーワード未入力なら全件表示される | 投稿が複数ある | 検索欄を空欄のまま検索する | 空欄 | 全ての投稿が表示される | | |
| 4-5 | 不正な`sort`パラメータでもエラーにならない | - | URLを直接`?sort=abc`のように書き換えてアクセスする | `sort=abc` | エラーにならず、新着順など通常表示にフォールバックする | | |

## 5. 管理者ログイン・権限

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 5-1 | 正しいパスワードでログインできる | ログイン画面を表示 | 正しいパスワードを入力しログイン | 正しいパスワード | 管理者画面(投稿一覧)に遷移する | | |
| 5-2 | 間違ったパスワードだとエラーになる | ログイン画面を表示 | 誤ったパスワードを入力しログイン | 誤ったパスワード | エラーメッセージが表示され、ログインできない | | |
| 5-3 | 未ログインで管理画面に直接アクセスできない | ログアウト状態(Cookie無し) | `/admin`に直接アクセスする | - | ログイン画面にリダイレクトされる | | |
| 5-4 | ログインから12時間後は再ログインが必要 | ログイン済み | Cookieの有効期限(12時間)経過後にアクセスする | - | ログイン画面にリダイレクトされる | | (時間がかかるため実施は任意) |

## 6. 管理者機能(投稿・コメント管理)

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 6-1 | 投稿を削除(ソフトデリート)できる | 管理者ログイン済み、公開中の投稿がある | 一覧から「削除」を押す | - | 状態が「削除済み」になり、タイムラインから消える | | |
| 6-2 | 削除した投稿を復元できる | 管理者ログイン済み、削除済みの投稿がある | 一覧から「復元」を押す | - | 状態が「公開中」に戻り、タイムラインに再表示される | | |
| 6-3 | 投稿内容を編集できる | 管理者ログイン済み、投稿が1件以上ある | 編集画面で本文を書き換えて保存する | 別の本文 | タイムラインの表示も更新される | | |
| 6-4 | 編集時も文字数上限(140文字)が効く | 管理者ログイン済み | 編集画面で141文字以上を入力し保存しようとする | 141文字の文字列 | エラーになり保存できない | | |
| 6-5 | コメントを削除できる | 管理者ログイン済み、コメントがある投稿がある | 編集画面からコメントを「削除」する | - | コメントが「削除済み」になり、タイムラインから消える | | |
| 6-6 | 削除したコメントを復元できる | 管理者ログイン済み、削除済みコメントがある | 編集画面からコメントを「復元」する | - | コメントが再びタイムラインに表示される | | |

## 7. エラーハンドリング

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 7-1 | バリデーションエラー時にエラー画面が表示される | - | devtools等で文字数制限を回避し、不正な内容を送信する | 141文字以上の本文 | Next.jsの標準エラー画面ではなく、独自のエラー画面(error.tsx)が表示される | | |
| 7-2 | エラー画面の「もう一度試す」で再実行できる | エラー画面が表示されている状態 | 「もう一度試す」ボタンを押す | - | 同じURLで処理が再実行される | | |

## 8. その他UI

| No | 確認内容 | 前提条件 | 操作手順 | 入力データ | 期待結果 | 実際の結果 | 判定 |
|----|----------|----------|----------|------------|----------|------------|------|
| 8-1 | スクロールすると「先頭に戻る」ボタンが表示される | 投稿が多く画面をスクロールできる状態 | 画面を下にスクロールする | - | 右下にボタンが表示される | | |
| 8-2 | 「先頭に戻る」ボタンでページの先頭に戻る | 8-1の状態でボタンが表示されている | ボタンを押す | - | 画面が先頭までスクロールされる | | |
| 8-3 | スマートフォンサイズでも崩れずに表示される | - | ブラウザの幅を狭くする(またはスマホで開く) | - | レイアウトが縦並びになり、はみ出しが無い | | |
