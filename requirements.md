# 匿名「ひとこと日記」共有アプリ 要件定義書（v4）

## 1. 概要

「今日あったちょっといいこと」を匿名で投稿し、みんなの投稿がタイムライン形式で流れる日記共有アプリ。
一般ユーザーはアカウント登録・ログイン不要で投稿・閲覧ができる。
管理者はパスワードでログインし、全投稿に対する検索・編集・削除（CRUD）を行える。

## 2. 機能要件

| No | 機能 | 対象 | 概要 |
|---|---|---|---|
| 1 | 投稿（Create） | 一般ユーザー | 「今日あったちょっといいこと」を短文（例：140文字程度）で投稿。ニックネームは任意入力（未入力なら「名無しさん」等で表示） |
| 2 | タイムライン表示・検索・並び替え（Read） | 一般ユーザー | 全員の投稿を新着順に一覧表示。投稿・コメントの本文およびニックネームに対するキーワード部分一致検索ができる（コメントに一致した場合もその投稿が表示される。削除済みコメントは対象外）。並び替えは「新着順」「リアクション数の多い順」を切り替えられる |
| 3 | リアクション | 一般ユーザー | 投稿ごとに複数種類のリアクション（いいね👍／わかる🙌／ほっこり🥰）を付与可能。同じ絵文字は1ブラウザにつき1回までで、もう一度押すと取り消せるトグル方式（Cookieで判別。後述） |
| 4 | 管理者ゲート | 管理者 | 固定パスワード（環境変数管理・ユーザー名なし）を入力すると管理画面にアクセスできる。一度正しいパスワードを入力したら、同じブラウザでは一定時間（12時間）再入力不要。ログアウトでき、同じIPからパスワードを連続して間違えるとロックされる |
| 5 | 投稿の検索（Read） | 管理者 | 管理画面内で本文・ニックネームをキーワード検索し、投稿を絞り込める（削除済み投稿も含めて検索可能） |
| 6 | 投稿の編集（Update） | 管理者 | 管理画面から投稿の本文・ニックネームを編集できる |
| 7 | 投稿の削除（Delete） | 管理者 | 管理画面から投稿をソフトデリートできる |
| 8 | コメント投稿（Create） | 一般ユーザー | 各投稿に対して短文コメントを付けられる。ニックネームは任意入力 |
| 9 | コメント表示（Read） | 一般ユーザー | 投稿の下にコメント一覧を表示 |
| 10 | コメントの削除（Delete） | 管理者 | 管理画面から不適切なコメントをソフトデリートできる |

## 3. 非機能要件・方針

- 一般ユーザーはアカウント登録・ログイン一切不要。ニックネームは投稿のたびに自由入力する表示ラベルに過ぎず、システム的な同一人物識別には使わない。
- 一般ユーザーは自分の投稿であっても編集・削除はできない（Update・Deleteは管理者のみが持つ権限）。
- コメントも投稿と同様にログイン不要・匿名（ニックネーム任意）。編集・削除は管理者のみが行える。
- リアルタイム更新は不要。ページ再読み込み・再訪問で最新状態が反映されればよい。
- 深刻な相談・自傷等の内容は投稿対象外とする方針をUI上で明示する。
- リアクションはトグル方式（同じ絵文字は1ブラウザにつき1回まで、もう一度押すと取り消し）にする。当初は「いいこと日記なのでリアクションが多く付くこと自体は望ましい」として二重押し防止をしない方針だったが、口頭ディフェンス後の要望で変更した。
  - 判別にはCookie`reacted`（有効期間2週間、`httpOnly`、`path: "/"`）に「投稿id:絵文字id」をカンマ区切りで保存する。管理者認証のCookie(`admin_auth`)とは役割・有効期間・有効範囲が異なるため別のCookieにしている。
  - 押していない組み合わせなら`Reaction`を1行追加（ON）、押した組み合わせなら同じ投稿・同じ絵文字の`Reaction`を1行削除（OFF）する。`Reaction`の行に持ち主の区別は無いため、DBのスキーマ変更は行っていない。
  - Cookieを削除する・別ブラウザやシークレットモードを使う・直接リクエストを送ると再度押せる。あくまでUI上の切り替えであり、厳密な不正防止ではない。またCookieを偽装して他人の付けたリアクションの行を消せる点は既知の制約とする。
- 管理者認証は、パスワードを環境変数の固定パスワードと照合するゲート方式とする。ユーザー名は持たない（管理者は1人想定）。
  - 当初は、認証済みフラグとして「パスワードのSHA-256ハッシュ値」を固定でCookieに保存する方式で実装していたが、口頭ディフェンス後の要望（セキュリティ強化）で、DBのセッション方式に変更した。旧方式には、環境変数が未設定だと空文字のハッシュで入れてしまう・Cookieが漏れると期限もログアウトも効かず再利用できる・ハッシュからパスワードを逆算される、といった弱点があった。
  - ログイン成功時に、ランダムな256ビットのトークンを発行し、Cookie(`admin_session`、有効期間12時間、`httpOnly`、`sameSite: "lax"`、`path: "/admin"`)に保存する。DBの`AdminSession`にはトークンのSHA-256ハッシュと期限を保存し、認証確認はCookieのトークンをハッシュ化してDBの期限内の行と照合する。期限はサーバー側で確認する。
  - ログアウトでDBのセッションを削除するため、トークンが別の場所にコピーされていても、その時点で無効になる。
  - パスワードの照合は、環境変数が未設定・空なら必ず拒否し、`timingSafeEqual`で比較する。
  - ログイン試行は`LoginAttempt`にIPアドレス単位で記録し、同じIPから15分間に5回を超えたらロックする（ロック中は正しいパスワードでも拒否し、ロックしたことを伝える。時間が経つと自動で解除）。試行を先に記録してから数えることで、同時に送られても通せるのは最初の5件までにしている。IPはVercelが設定する`x-forwarded-for`から取得する。
  - 限界: IPを変えながら試す攻撃には効きにくい。同じ公開IPを共有するネットワークからは、同じ回数制限が共有される。パスワード自体の強度（長さ・複雑さ）は今回は変更していない。トークンが盗まれた場合は、ログアウトするまで（または期限まで）有効。

## 4. 画面一覧

1. タイムライン画面（投稿一覧＋検索欄＋並び替え切替＋リアクションボタン＋各投稿のコメント欄）
2. 投稿画面（ひとこと投稿フォーム、ニックネーム任意入力）
3. 管理者パスワード入力画面（ゲート）
4. 管理者画面（投稿一覧・検索・編集・削除）※ Read/Update/Deleteをここに集約
5. 投稿編集画面（管理者画面から遷移、`/admin/posts/[id]/edit` 相当）

## 5. テーブル設計

### Post（投稿）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| nickname | String（`@default("名無しさん")`、NOT NULL） | 投稿時に自由入力（任意、未入力ならDBデフォルト値が入る） |
| content | String | 投稿本文（140文字以内） |
| createdAt | DateTime | 投稿日時 |
| updatedAt | DateTime | 管理者による編集日時 |
| deletedAt | DateTime? | 管理者による削除日時（null＝未削除） |

※ `nickname`は当初`String?`（NULL許容）を想定していたが、「未入力＝null」のままだと検索欄で「名無し」等を検索してもヒットしないUX上の問題があったため、DBの`@default("名無しさん")`で必ず文字列が入る設計に変更した（新規作成時のみ有効。更新時はコード側で明示的にデフォルト値と同じ文字列をセットしている）。

### ReactionType（リアクション種類マスタ）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| name | String（UK） | いいね／わかる／ほっこり（確定）。重複投入防止のためユニーク制約あり |
| emoji | String（UK） | 👍／🙌／🥰（確定）。同上 |

### Reaction（リアクション）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| postId | Int (FK → Post.id) | 対象の投稿 |
| reactionTypeId | Int (FK → ReactionType.id) | リアクション種類 |
| createdAt | DateTime | |

※ リアクションのトグル化は、匿名IDカラムを追加せずCookieだけで実現している（3章参照）。

### Reply（コメント）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| postId | Int (FK → Post.id) | コメント対象の投稿 |
| nickname | String（`@default("名無しさん")`、NOT NULL） | コメント時に自由入力（任意、未入力ならDBデフォルト値が入る。Postと同じ理由） |
| content | String | コメント本文（30文字以内） |
| createdAt | DateTime | コメント日時 |
| deletedAt | DateTime? | 管理者による削除日時（null＝未削除） |

### 管理者認証について

管理者のユーザー情報用のテーブルは作らない。環境変数（例：`ADMIN_PASSWORD`）に固定パスワードを1つ持ち、
入力されたパスワードと照合する。認証に成功したら、ランダムなトークンを発行してCookieに持たせ、
その記録を下記の`AdminSession`テーブルで管理する（詳細は3章の管理者認証の項を参照）。

一般ユーザー向けの`User`テーブルも作らない（要件2章の通り、アカウント機能自体が存在しないため）。

### AdminSession（管理者のログインセッション）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| tokenHash | String（UK） | Cookieに持たせるトークンのSHA-256ハッシュ。トークンそのものは保存しない（DBが漏れてもログインに使えないようにするため）。ユニーク制約が検索用の索引を兼ねる |
| expiresAt | DateTime | 有効期限（ログインから12時間後）。認証確認のたびにサーバー側で確認する |
| createdAt | DateTime | |

### LoginAttempt（ログイン試行の記録）
| カラム | 型 | 説明 |
|---|---|---|
| id | Int (PK) | |
| ip | String | 試行元のIPアドレス。回数制限の単位 |
| createdAt | DateTime | 試行の日時。直近15分間の件数を数えるのに使う |

※ `ip`と`createdAt`に複合インデックスを付けている（IPごとの直近の件数を速く数えるため）。ログイン成功時にそのIPの記録を削除し、期間外の古い記録はログインのたびに削除する。

## 6. CRUD対応表

書籍管理アプリの一覧・詳細・登録・編集の型を踏襲しつつ、権限を一般ユーザー／管理者で分けている。

| 操作 | 画面 | 権限 |
|---|---|---|
| Create | 投稿画面 | 一般ユーザー |
| Read（一覧・検索・並び替え） | タイムライン画面 | 一般ユーザー |
| Read（検索・削除済み含む） | 管理者画面 | 管理者 |
| Update | 投稿編集画面 | 管理者 |
| Delete | 管理者画面（一覧から操作） | 管理者 |
| Create（コメント） | タイムライン画面 | 一般ユーザー |
| Read（コメント） | タイムライン画面 | 一般ユーザー |
| Delete（コメント） | 管理者画面 | 管理者 |

## 7. 検索・並び替えの実装メモ

- 検索：`Post.content`・`Post.nickname`に加え、コメント(`Reply.content`・`Reply.nickname`)に対するOR条件の部分一致検索（Prismaの`contains`、大文字小文字を区別しない`mode: "insensitive"`を使用）。コメント側は`replies: { some: { deletedAt: null, OR: [...] } }`で「削除されていないコメントが1件でも一致する投稿」を対象にしている
- 並び替え：
  - 新着順 → `orderBy: { createdAt: "desc" }`
  - リアクション種類ごとの多い順（👍が多い順／🙌が多い順／🥰が多い順、と種類ごとに個別選択できる）→ 当初想定していたPrismaの`_count`によるリレーション件数ソートは「全種類合計」しか数えられず、種類ごとの件数でソートできないため使っていない。代わりに、リアクションの件数は`prisma.reaction.groupBy()`でDB側で「投稿×絵文字」ごとに集計し、`createdAt: "desc"`で取得した投稿の配列を、その集計結果から引いた選択中の種類の件数でJS側の`.sort()`して並び替える方式で実装した（当初は生のリアクション行を全件取得してJSの`.filter().length`で数えていたが、リアクションが増えると転送量・計算量が増えるため`groupBy`に変更した）
- 新テーブルの追加は不要。既存の`Post`・`Reaction`の組み合わせだけで実現できた

## 8. 今後の検討事項（未確定）

（当初ここに挙げていた項目はすべて決定済みのため、上記の各章に反映した。投稿本文の文字数上限は140文字、コメントは30文字、リアクション種類は いいね👍／わかる🙌／ほっこり🥰、二重押し防止は導入しない、認証Cookieはハッシュ値照合方式で12時間有効、とそれぞれ確定している。）
